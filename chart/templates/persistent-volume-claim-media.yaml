{{- if .Values.persistence.media.enabled -}}
---
kind: "PersistentVolumeClaim"
apiVersion: {{ .Values.apiVersions.persistentVolumeClaim | quote }}
metadata:
  name: {{ .Values.persistence.media.pvcName | quote }}
  namespace: {{ include "names.namespace" . | quote }}
  labels:
    {{- include "labels.commonLabels" . | nindent 4 }}
  {{- with .Values.persistence.media.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  accessModes:
  {{- range .Values.persistence.media.accessModes }}
    - {{ . | quote}}
  {{- end }}
  resources:
    requests:
      storage: {{ .Values.persistence.media.size | quote }}
  storageClassName: {{ .Values.persistence.media.storageClass | quote }}
...
{{- end }}
