{{- if .Values.persistence.consume.enabled -}}
---
kind: "PersistentVolumeClaim"
apiVersion: {{ .Values.apiVersions.persistentVolumeClaim | quote }}
metadata:
  name: {{ .Values.persistence.consume.pvcName | quote }}
  namespace: {{ include "names.namespace" . | quote }}
  labels:
    {{- include "labels.commonLabels" . | nindent 4 }}
  {{- with .Values.persistence.consume.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  accessModes:
  {{- range .Values.persistence.consume.accessModes }}
    - {{ . | quote}}
  {{- end }}
  resources:
    requests:
      storage: {{ .Values.persistence.consume.size | quote }}
  storageClassName: {{ .Values.persistence.consume.storageClass | quote }}
...
{{- end }}
